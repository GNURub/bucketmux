[workspace]
resolver = "2"
members = [
  "uppercase-transform",
  "image-classifier",
  "embedding-matcher",
]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
